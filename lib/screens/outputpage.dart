import 'package:flutter/material.dart';

class OutPutPage extends StatelessWidget {
  const OutPutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Upscalec image"),
      ),
      body: Container(),
    );
  }
}
